[package]
name = "strata-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
strata-core = { path = "../strata-core" }

[dev-dependencies]
tempfile = "3"
