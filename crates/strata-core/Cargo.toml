[package]
name = "strata-core"
version = "0.2.0"
edition = "2021"

[dependencies]
zstd = { workspace = true }
xxhash-rust = { workspace = true }
thiserror = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
proptest = "1"
